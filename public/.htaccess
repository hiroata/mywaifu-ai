# Next.jsアプリ用の.htaccessファイル

# デフォルトインデックスファイルの設定
DirectoryIndex index.html index.php

<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # HTTPSへのリダイレクト
  # RewriteCond %{HTTPS} off
  # RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
  
  # ファイルやディレクトリが存在する場合は、それを表示
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
    
  # Next.jsの_nextディレクトリに対するリクエストを処理
  RewriteRule ^_next/(.*) .next/$1 [L]
  
  # APIリクエスト
  RewriteRule ^api/(.*) /api/$1 [L]
  
  # Next.jsのサーバーサイドレンダリング用
  # 存在しないパスはすべてindex.phpにリダイレクト（Node.jsサーバーが処理）
  RewriteRule ^ index.php [L]
</IfModule>

# ブラウザキャッシュの設定
<IfModule mod_expires.c>
  ExpiresActive On
  
  # 画像などの静的ファイルは1年間キャッシュ
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  
  # CSSとJavaScriptファイルは1週間キャッシュ
  ExpiresByType text/css "access plus 1 week"
  ExpiresByType application/javascript "access plus 1 week"
  
  # HTMLとデータファイルはキャッシュしない
  ExpiresByType text/html "access plus 0 seconds"
  ExpiresByType application/json "access plus 0 seconds"
</IfModule>

# GZIPによる圧縮
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/plain
  AddOutputFilterByType DEFLATE text/html
  AddOutputFilterByType DEFLATE text/css
  AddOutputFilterByType DEFLATE application/javascript
  AddOutputFilterByType DEFLATE application/json
  AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>
